import type {ElementType} from "react"
import {Layers, Brain, Zap, Settings} from "lucide-react"

// 图标映射
export const iconMap: Record<string, ElementType> = {
    Layers,
    Brain,
    Zap,
    Settings,
}
